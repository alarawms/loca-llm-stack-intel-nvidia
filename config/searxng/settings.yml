use_default_settings: true

server:
  secret_key: "ai-stack-searxng-local"
  limiter: false
  image_proxy: true

search:
  safe_search: 0
  autocomplete: "google"
  default_lang: "en"
  formats:
    - html
    - json
