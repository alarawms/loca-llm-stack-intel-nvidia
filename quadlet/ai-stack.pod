[Unit]
Description=Local AI Stack (LLM + STT + TTS + WebUI + Search)

[Pod]
PodName=ai-stack
# No explicit host IP — pasta handles dual-stack (IPv4 + IPv6) automatically
# Services still accessible on all interfaces (LAN, Tailscale, localhost)
PublishPort=11434:11434
PublishPort=9100:9000
PublishPort=5002:5002
PublishPort=8080:8080
PublishPort=8888:8888
PublishPort=18789:18789
PublishPort=18790:18790

[Install]
WantedBy=default.target
